
### LOCKOUT-PROOF (design[0])
_LOCKOUT-PROOF ARCHITECTURE for GAW ModTools (manifest on disk is v10.21.0, one ahead of the v10.20.0 in the brief). I read every auth surface end-to-end and the recon map is accurate. Root cause of the operator's hard lockout is FOUR converging gaps, all fixable EXTENSION-SIDE with ZERO worker deploy (so nothing is classifier-gated): (1) `_rpcWorkerCall` (background.js:2378-2420) has no 401 handling -> a rotated/stale token fails silently forever; (2) on ANY whoami failure the popup dumps the op_
- **[CRITICAL/M/dep=False] Add an encrypted LOCAL TOKEN BACKUP (gam_token_backup_v1) written on every successful save/rotation — separate from gam_settings so no settings-read can echo it**
  - Add `async function _writeTokenBackup(){...}` in background.js (near _persistRotatedToken ~L2328). It reads secretCache, and writes a NEW top-level key `chrome.storage.local.set({ gam_token_backup_v1: { worker_enc, lead_enc, savedAt, ver } })` where worker_enc/lead_enc are `await _cryptEncrypt(token)` blobs (reuse background.js:183) PLUS a plaintext fallback field `worker_pt`/`lead_pt` guarded behind the SAME plaintext-is-already-here reality (team token is already plaintext in gam_settings:2559, so a backup plaintext is not a NEW exposure — but keep lead backup encrypted-with-plaintext-fallba
  - _ev: Team token persisted plaintext+encrypted at background.js:2559-2568 (authValidateToken); lead token at background.js:3160-3172; rotation at _persistRotatedToken_
- **[CRITICAL/M/dep=False] AUTO-RESTORE from backup when loadSecrets finds the vault empty but a backup exists — self-heal the exact wiped-vault lockout**
  - In loadSecrets, after the gam_settings block yields empty (background.js:439, where s={workerPlain,leadPlain} are both ''), add a THIRD fallback tier: read `gam_token_backup_v1`; if present, decrypt worker_enc/lead_enc (or take worker_pt/lead_pt), shape-validate via __isValidTokenOrEmpty (background.js:958), and on success (a) set secretCache, (b) RE-HYDRATE the live vault by calling the same persist path (authValidateToken's storage write, or a shared _persistTeamToken helper) so gam_settings is repopulated, and (c) console.warn + _maintAppendDiag('auth.autorestore','ok'). Order: session -> g
  - _ev: loadSecrets (background.js:388-475) reads session then gam_settings local; if BOTH token fields are empty it sets secretCache to {'',''} (background.js:442-445)_
- **[HIGH/M/dep=False] Mirror the token backup to chrome.storage.sync as a SECOND source of truth that survives a full storage.local wipe — no new manifest permission needed**
  - In _writeTokenBackup (finding above) ALSO write `chrome.storage.sync.set({ gam_token_backup_v1: {worker_enc, lead_enc, savedAt, ver} })`. CRITICAL no-leak constraint: storage.sync the ENCRYPTED blobs ONLY (worker_enc/lead_enc), NEVER plaintext — sync roams to Google's servers, so plaintext there is a real exposure where local plaintext is not. The device CryptoKey is non-extractable and IDB-bound (background.js:167-172) and does NOT roam, so a synced blob is only decryptable ON THIS DEVICE — acceptable: it is a same-device wipe-survival mirror, not a cross-device transfer. Respect the ~8KB syn
  - _ev: manifest.json:21-25 grants `storage` (covers storage.sync) — confirmed NO storage.sync usage anywhere in popup.js (grep clean) or background, so this is greenfi_
- **[CRITICAL/M/dep=False] 401-STORM SELF-DETECTOR in _rpcWorkerCall — flip a gam_auth_failed flag so the popup shows RECOVERY, not NEW-MOD onboarding**
  - In _rpcWorkerCall, after `const r = await fetch(...)` (background.js:2401-2406), detect `r.status === 401`: (1) GATE on init — skip if `_initReady`(background.js:480) hasn't resolved, so cold-boot races (background.js:477-489) don't false-positive; (2) one-shot cache-bust + reload: `secretCache={'',''}; await loadSecrets();` then RETRY the fetch ONCE (covers the case where storage.local has a newer token than RAM — the v9.2.2 class) AND triggers the new auto-restore; (3) if the retry still 401s, increment a `gam_auth_fail_count` and set `chrome.storage.local.set({ gam_auth_failed:{ts,path,coun
  - _ev: _rpcWorkerCall background.js:2378-2420 returns {ok:false,status:401} with ZERO reaction — no cache-bust, no re-load, no flag. The v9.2.2 stale-vault class is ac_
- **[CRITICAL/M/dep=False] Make __applyTierGate route to a RECOVERY state on 401, not first-run NEW-MOD onboarding — the operator must never be told to onboard when they are already a mod**
  - Add a fourth state to the __tokSetState machine (popup.js:210) e.g. 'recovery' that renders the PANIC RE-AUTH panel (next finding) instead of onboarding. In __applyTierGate, branch on the failure kind: (a) read `gam_auth_failed` + `tokensStatus`(popup.js:1743) — if a backup exists OR hasTeamToken was ever true, route to 'recovery', NOT 'first-run'; (b) ONLY route to true 'first-run' when there is genuinely no token and no backup (a real new mod). Distinguish 401 (token dead -> recovery) from status 0/timeout/5xx (transient -> keep current state + 'Reconnecting…', which the late-resolve path po
  - _ev: __applyTierGate popup.js:2030-2034 (whoami !ok), :2090-2095 (catch), and the 5s timeout :2008-2011 ALL call `__tokSetState('first-run')` -> NEW-MOD onboarding (_
- **[HIGH/L/dep=False] POPUP PANIC RE-AUTH panel — one-click Restore-from-backup + always-reachable lead escape hatch + import RECOVERY-TOKEN file**
  - Build a `#tokStateRecovery` block in popup.html + wire in popup.js with THREE actions: (1) 'Restore last working token' button -> new RPC `restoreFromBackup` that calls _writeTokenBackup's inverse (read gam_token_backup_v1 local-or-sync, decrypt, promote to vault via the authValidateToken persist path, then re-run __applyTierGate). Show the backup's savedAt so the operator trusts it. (2) An ALWAYS-VISIBLE 'I'm the lead — paste lead token' field in this recovery state, decoupled from whoami (next finding). (3) 'Import recovery file' using an `<input type=file>` to read the saved RECOVERY-TOKEN 
  - _ev: Recovery affordances today are minimal: a CRITICAL text string on rotation-save-fail (popup.js:2972-2977) and a 'no invite staged' dead-end note (popup.js:3620)_
- **[CRITICAL/M/dep=False] Decouple lead-token recovery from whoami AND from the team-token precondition — break the catch-22 that forced the wrangler path**
  - Two extension-side changes: (1) In the NEW recovery state, render the lead-token input UNCONDITIONALLY (it is on the recovery surface, which only shows when auth has failed — so no risk of showing it to a healthy non-lead, satisfying the v9.3.16 'never show #leadSection to non-leads' intent at popup.js:1952 because this is a different, failure-only container). (2) Relax authValidateLeadToken's precondition: when no team token is present, validate the lead token STANDALONE against a lead-gated endpoint that does NOT require X-Mod-Token. Worker `requireLeadAuth` Path A trusts env LEAD_MOD_TOKEN 
  - _ev: authValidateLeadToken HARD-REQUIRES a stored team token: background.js:3140 `if (!teamTok) return {ok:false,'team token not set -- save team token first'}` and _
- **[HIGH/S/dep=False] Fix the lead token being stored ENCRYPTED-ONLY — it dies on SW eviction while the team token survives, an asymmetric lockout vector**
  - Make the lead path symmetric with the team path: in authValidateLeadToken, do NOT delete plaintext — write BOTH `base.leadModToken = candidate` AND `base.leadModToken_encrypted = await _cryptEncrypt(candidate)` (mirror background.js:2559-2561). Same in any lead rotation path. This single change removes a whole class of 'lead token silently dead after a Chrome restart' that looks identical to a real lockout. The backup/auto-restore findings then cover it doubly. (Security note: lead plaintext at rest is the SAME exposure level already accepted for the team token, and the threat model already ke
  - _ev: authValidateLeadToken background.js:3163 does `delete base.leadModToken` (strips plaintext, keeps only leadModToken_encrypted). But the v10.11.1 HOTFIX explicit_
- **[MED/S/dep=False] Unify the four divergent token-shape predicates so a valid worker-minted token can never be saved-but-rejected (phantom lockout)**
  - Export ONE shared predicate matching exactly what the worker's randomToken can emit (recon: worker randomToken(48), alphabet incl. _-). Make popup __isTokenShape (popup.js:1690), authValidateToken (background.js:2527), saveToken (popup.js:1837), and the storage.onChanged guard (background.js:1016) all call the SAME function as __isValidTokenOrEmpty (background.js:958). Confirm randomToken cannot produce a leading/trailing dash that the strict guard then rejects; if it can, either loosen the guard or constrain randomToken (worker-side, deploy-gated). This is defense for the recovery paths too: 
  - _ev: background.js:957 __TOKEN_SHAPE_RE plus EXTRA rules (no leading/trailing dash/underscore, must contain letter AND digit, background.js:962-967); popup.js:1690 __
- **[MED/S/dep=False] Gate the new 401-storm flag behind _initOnce so cold-boot/alarm races don't false-trip the recovery banner**
  - In the new 401 detector (finding #4), short-circuit when `_initReady` is unresolved OR when the cache-bust+reload+retry has not yet been attempted — only set gam_auth_failed AFTER loadSecrets/auto-restore ran and the RETRY still 401'd. Also audit each alarm handler (AI_USERS_SCAN_ALARM background.js:59, AUTO_POLL_ALARM background.js:55) to `await _initOnce()` before its first _rpcWorkerCall. This ensures the recovery UX only appears for a GENUINE dead token, never a 200ms cold-boot window — otherwise the operator would see scary 'token rejected' banners on every browser restart.
  - _ev: _initOnce (background.js:481-489) gates RPC readiness; comments at background.js:1930 and 4367 acknowledge 'alerts race an empty vault -> silent 401s.' The 30-m_
- **[MED/S/dep=False] saveToken invite-vs-token auto-detect already 401-specific, but harden against the input being cleared before re-stage and against non-401 worker hiccups**
  - Minor hardening that compounds with the recovery design: (1) before clearing tokenInput, stash the raw value so the recovery panel can re-offer it ('we couldn't verify — your pasted value is preserved'); (2) ensure the rollback at popup.js:1882 ONLY fires on an explicit 401/token_invalid, leaving a 429/5xx/timeout token saved with a 'verifying later' note (the storm detector + auto-restore then cover it). This keeps a valid token pasted during a worker hiccup from being thrown away mid-recovery.
  - _ev: saveToken popup.js:1879-1898 re-routes to invite-claim ONLY on probe.status===401 (good — already not status 0). BUT the input is cleared at popup.js:1851 in th_
- **[LOW/S/dep=False] Add a passive backup-health indicator to tokensStatus so the operator can SEE they are protected — and a 'last backup' line in the healthy state**
  - Extend the tokensStatus response (background.js:2234-2238) with booleans `hasLocalBackup`, `hasSyncBackup`, and `backupSavedAt` (timestamp only — NEVER token bytes), read from gam_token_backup_v1 (local + sync). Surface a small green 'Recovery backup: saved <date> (local + synced)' line in the healthy Tokens state and a louder amber 'No recovery backup yet — click Save recovery file' if absent. This makes lockout-resistance VISIBLE and nudges the operator to keep the on-disk RECOVERY-TOKEN file fresh. Strictly read-only/diagnostic — honors no-secret-echo (booleans + timestamp only) and HI-1 (n
  - _ev: tokensStatus background.js:2212-2239 already returns presence + age booleans WITHOUT echoing token material — the canonical safe-signal pattern. It currently ha_

### NEXT-WAVE FEATURES (design[3])
_RANKED next-wave build queue for GAW ModTools, grounded in the REAL code (codegraph wasn't initialized for this project, so every cite is from direct file reads). Two state corrections to the brief first: (1) the extension is v10.21.0, not v10.20.0 (manifest.json) — v10.20 Bot Raid Shield UI shipped, then a lead-lockout recovery tool landed; (2) R6/R8 are NO LONGER untested — _braid_r6r8_smoke_test.mjs was written TODAY and passes 35/35 with HI-1 hard-verified (mock D1 throws on any ban-table wr_
- **[HIGH/M/dep=False] In-feed slop badge — client-side content-quality flag riding the existing per-post action strip**
  - This is the #1 build and it ships WITHOUT a worker deploy. The recon framed a quality gate as worker-side L-effort/deploy-gated; the real code makes it extension-first and deployable now. `buildActionStrip` already runs on EVERY `.post, .comment` via the universal mutation hook (modtools.js:11979) and `injectInto` already walks `.post[data-id]` per-post (modtools.js:3899-3919). Add a `scorePostQuality(el)` T0 heuristic computed client-side from the DOM the mod is already looking at — length, link-only/link-density, all-caps ratio, emoji density, low-effort one-liner, duplicate-title fuzzy matc
  - _ev: buildActionStrip per-post at modtools.js:11979; injectInto walks .post[data-id] at modtools.js:3899-3919; client T0 fold target at gaw-mod-proxy-v2.js:3555-3583_
- **[HIGH/M/dep=False] First-post screening in the FIREHOSE client loop — catch new-account slop/bots at the door**
  - The FIREHOSE client crawler already scrapes every /new post and pushes to /gaw/posts/ingest (modtools.js:30439-30586, FIREHOSE_BATCH=40). Tap that exact loop: when a scraped post's author is a brand-new account (the worker already computes is_new_account<14d and account_age_days everywhere — gaw-mod-proxy-v2.js:1430, 5944), run the same client T0 quality heuristic from feature #1 plus a new-account-specific bot check, and on a trip place the author in SUS with a 'NEW+FLAGGED' badge so the robot-icon SUS popover (modtools.js:20697, 22797) surfaces them BEFORE the post farms karma. Pure SUS plac
  - _ev: FIREHOSE client crawler pushing to /gaw/posts/ingest at modtools.js:30439-30586; is_new_account<14d + account_age_days at gaw-mod-proxy-v2.js:1430,5944; SUS pop_
- **[HIGH/M/dep=True] Curator scorecard — 'is the site getting cleaner?' + false-positive/reversal rate from the disposition data already being written**
  - There is no instrument telling the curator whether the moat is working: handleModStats only does 24h action counts, KV-cached (gaw-mod-proxy-v2.js:1156-1180). Build a scorecard panel (extend the existing SH2 status tiles at modtools.js:22509 — same surface as the FIREHOSE tile — do NOT build a new surface) showing 7/30d trends of new-account SUS-flag rate, raid frequency, repeat-offender ratio, and CRITICALLY a false-positive / ban-reversal rate. The raw signal already exists: handleRaidDispositionFeedback writes flush(0.7)/remove(0.3) dispositions (gaw-mod-proxy-v2.js:4924) and R8 is now smok
  - _ev: handleModStats 24h-counts-only at gaw-mod-proxy-v2.js:1156-1180; disposition rows written at handleRaidDispositionFeedback gaw-mod-proxy-v2.js:4924 (R8 smoke 35_
- **[HIGH/M/dep=True] Ban-evasion sentinel — sock-puppet detection via the co-commenter graph that lookalikes already half-builds**
  - A ruthless curator's top frustration is banned users re-registering, and today only handleUsersLookalikes exists (gaw-mod-proxy-v2.js:13503) — and it is feature-flagged OFF (LOOKALIKES_FEATURE!=='true' returns 404) and only does a co-commenter self-join, no SUS write, no comparison against the ban list. Upgrade it into an active sentinel: when a fresh account appears in FIREHOSE, cross-reference recently-banned/Death-Row usernames with Levenshtein name distance + behavioral fingerprint (same posting cadence via handleModUserCadence at 18529, same co-commenter cluster from the existing gaw_comm
  - _ev: handleUsersLookalikes flag-gated-off + co-commenter self-join only at gaw-mod-proxy-v2.js:13503-13540; gaw_comments(post_id,author,is_removed) schema at 15522; _
- **[MED/M/dep=True] Self-improving Auto-DR rules — per-rule hit/accuracy column fed by the disposition signal**
  - Auto-DR regex/wildcard rules (modtools.js:15419-15429, synced via /mod/dr-rules at gaw-mod-proxy-v2.js:18388, with a 'Run all rules now' sweep at 15415) are blind: the curator writes patterns with zero feedback on which catch real bad actors vs sit idle vs false-positive. handleAiTardsSuggest already proposes patterns from the firehose (/ai/tards/suggest at 18554). Close the loop: track per-rule hit counts + downstream disposition (did the FLUSH stick or get overturned, joining the R8 disposition table at 4924), render a hit/accuracy column next to each rule in the Auto-DR sidebar, and have th
  - _ev: Auto-DR rules UI at modtools.js:15419-15429 + 'Run all rules now' sweep at 15415; /mod/dr-rules at gaw-mod-proxy-v2.js:18388; handleAiTardsSuggest at 18554; dis_
- **[MED/M/dep=False] Brigade-cluster bulk triage — operationalize the brigade detector that already runs every cron**
  - brigadeTick runs every cron in soak-mode with hard alerts gated off (gaw-mod-proxy-v2.js:13858, BRIGADE_HARD_ALERTS_ON check at 13951) and handleModBrigadeNewCluster clusters coordinated brigades (14027) — but the output is a passive alert, not an actionable surface. A curator facing a brigade needs to act on the WHOLE cluster at once. Extend the EXISTING SUS popover (modtools.js:20697 — the v10.20 bot-raid bulk-flush already proved layering on _showSusPopover at 21146-21163 is the right move; do NOT build a new surface) to render a detected cluster as a group, pre-select its members with evid
  - _ev: brigadeTick soak-mode at gaw-mod-proxy-v2.js:13858 + BRIGADE_HARD_ALERTS_ON gate at 13951; handleModBrigadeNewCluster at 14027; bot-raid bulk-flush layered on __
- **[MED/M/dep=True] Ban-appeal intake queue — turn the appeal promise the ban templates already make into a measured signal**
  - Every AI ban message ends with 'appeals are via modmail' (gaw-mod-proxy-v2.js:9827) but there is NO appeal queue — appeals drown in the general modmail stream (handleModmailSync at 18453, handleModmailRecent at 18562) with no link to the originating ban/evidence, and no /appeal* route exists. Build /mod/appeals (list/resolve) that auto-detects appeal-shaped modmail threads, links each to the precedent ban (handlePrecedentFind at 17050), and surfaces a focused triage panel with original ban reason + evidence + one-click uphold/overturn. Overturn writes to the disposition-feedback table (gaw-mod
  - _ev: ban message 'appeals are via modmail' at gaw-mod-proxy-v2.js:9827; modmail handlers at case table 18453/18562; handlePrecedentFind at 17050; disposition sink at_
- **[HIGH/S/dep=True] Deploy + live-fire R6/R8 (smoke is already 35/35 green — this is operator-attended deploy, not a build)**
  - STATE CORRECTION to the recon: _braid_r6r8_smoke_test.mjs was written TODAY (Jun 6 22:01) and passes 35/35 with the HI-1 invariant hard-verified (the mock D1 throws if R6/R8 touches any ban table). So the recon's 'NOT smoke-tested' is now stale — smoke is green. The ONLY remainder is the classifier-gated `wrangler deploy` + a live curl against the deployed /raid/intake and /raid/disposition-feedback, and that must be operator-attended because the lead just got HARD LOCKED OUT (token rotated to 401, ~20-step recovery). Do NOT auto-deploy into a known-fragile token state. This is the de-risked u
  - _ev: _braid_r6r8_smoke_test.mjs RESULT 35 passed 0 failed (HI-1 mock-DB-throws-on-ban-table verified); handlers wired at case table gaw-mod-proxy-v2.js:18401-18402; _
- **[LOW/S/dep=True] WORKER_VERSION drift + classifier-deploy reconciliation (hygiene the lockout exposed)**
  - The worker's WORKER_VERSION constant is pinned at '10.18.1' (gaw-mod-proxy-v2.js:87) while the extension shipped v10.21.0 and R6/R8 are v10.20.0 features — the deployed worker version string will lie about what's running, which matters when diagnosing the NEXT lockout (the operator pastes /admin/health and sees a stale version). Bump WORKER_VERSION as part of the R6/R8 deploy, and add the version to the deploy smoke assertion so the smoke test fails if the deployed string doesn't match source. Low severity, but the lockout recovery took ~20 steps partly because state was ambiguous — every vers
  - _ev: WORKER_VERSION = '10.18.1' at gaw-mod-proxy-v2.js:87; extension manifest at v10.21.0; R6/R8 are v10.20.0 features at case table 18401-18402_