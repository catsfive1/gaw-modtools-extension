# GAW ModTools — HANDOFF 2026-06-30

**Resume cold from this doc.** Companion: `docs/FEATURES_MATRIX_v10.31.md` (requested vs delivered) · `docs/UI-STORM-2026-06-22.md` (storm top-15) · `docs/inpage-redesign/` (redesign brief + tokens).

---

## Headline — current state (07-01)

**Merged, HI-1 resolved, /users P0 closed.** `inpage-ui-storm` merged to `master` (was frozen since 06-19). Everything below v10.36.0 now ships directly on `master`. Current version **v10.36.4**. A parallel session (with Opus 4.8 doing recon/planning per `docs/OPUS48_BRAINSTORM_BRIEF.md`) shipped v10.36.1–v10.36.4 same-day: a TDZ hotfix, a P0 fix for a gap in this session's own v10.36.0 `/users` work, the HI-1 ban-wiring decision executed, and a Wave-2 prereq helper. Only Wave 2's full bar regrouping remains open. **Two research findings from this session, still needing a decision:** (1) `ai_used`/`ai_tone` data pipe works but nothing consumes it — real fix is V11-scale (see Open items); (2) worker-side auto-unsticky (both the CS-scan-report handler and the cron scanner) requires age **AND** votes to queue a candidate, while personal-mode + the UI's own label promise age **OR** votes — looks like a deliberate conservative choice for unattended team-wide action, but was never reconciled with the docs/personal-mode behavior. Needs Commander's call, not a unilateral fix (see Open items).

## Timeline (cumulative)
| Date | Ref | Event |
|---|---|---|
| 06-19 | `ec0e81a..4e65bcc` | inpage-ui WP-01..13 shipped (token system, 13 components, a11y) |
| 06-19 | `74a0109..c7b3b09` | same-day review-fix passes |
| 06-19 | `0bc8146` | fix: ESC-confirm DOM orphan → added to `closeAllPanels` SEL |
| 06-19 | `633dcbb` | safety net: tag `safety/v10.29.0-pre-ui-storm` + fork `inpage-ui-storm`; master FROZEN |
| 06-20 | — | post-delivery docs `/docs/inpage-redesign/` (brief 729L, tokens 228L, prototypes) |
| 06-22 | `3a53a61` | 21-agent UI-STORM synthesis → top-15, 3 waves, 7 fluff cut |
| 06-22 | `317300b` | fix STORM #2 — toolbar artifact (`_gamLinkPreviewEl` orphan) |
| 06-22 | `b6dd92c` | fix STORM #1 pt1 — snack-gate on `r.ok` + SUS live rewire (smoke 11/11) |
| 06-22 | `97321f5` | fix STORM #11 — modmail wrong-user on thread-switch |
| 06-22 | `84a97e4` | v10.30.0 — feed-observer de-jank (smoke 5/5) |
| 06-22 | `ef47804` | v10.31.0 — ROOT-CAUSE profile content-eating fix (smoke 16/16) |
| 06-30 | `6f5abc?` | v10.32.0 — STORM #6: overlay dismiss + z-index drop (`var(--z-modal)`) |
| 06-30 | `168b78c`→ | v10.33.0 — STORM #14: showModal XSS footgun closed (`titleIsHtml` opt-in) |
| 06-30 | `38a58e5` | v10.34.0 — STORM #15: collapse racing `--bb-*` :root blocks |
| 06-30 | `3803586` | v10.35.0 — STORM #12: passive last-3-actions strip in collectDebugSnapshot |
| 06-30 | `770e0fe` | v10.36.0 — /users selector hardening (5 sites) + cluster "Death Row all N" button |
| 06-30 | `7fda929` | merge `inpage-ui-storm` → `master` (Wave 1 + Wave 3, v10.29.0→v10.35.0) |
| 07-01 | `86b0fa9` | v10.36.1 — HOTFIX TDZ crash on `MSG_QUEUE_KEY` (same class as v10.6.1 `FEATURE_FLAGS`) |
| 07-01 | `10826de` | test: regression coverage for bug #8 (MSG_QUEUE_KEY TDZ); tracked in `gaw-audit` D1 `bug_reports` |
| 07-01 | `94c3108` | v10.36.2 — P0 HOTFIX: `/users` mount-fail silently swallowed + hard `spans.length<2` gate discarded rows before v10.36.0's own fallback ran (Opus 4.8 recon) |
| 07-01 | `77244a6` | v10.36.3 — P1 HI-1: `instantPermaBan`/`batchBanUsers` now route through `addToDeathRow(...,0,...)` + explicit reaper fire ("guarded break-glass" — Commander decision, no more direct `executeBan`) |
| 07-01 | `3965ce5` | v10.36.4 — P2 prereq: `openCategoryMenu()` shared popover helper for the upcoming bar regroup (purely additive) |
| 07-01 | `c950e88` | docs: stale auto-unsticky comment fix (no functional change) |

## Regressions & fix status
| Issue | Status |
|---|---|
| ESC-confirm DOM orphan | ✅ FIXED `0bc8146` |
| Link-preview artifact stuck | ✅ FIXED `317300b` |
| Thread-watch silent-success snacks (4 unregistered RPCs) | ⚠️ PARTIAL `b6dd92c` — honest now; Ban/Watch wiring pending HI-1 |
| Modmail banning wrong user on thread-switch | ✅ FIXED `97321f5` |
| Feed-observer jank | ✅ FIXED `84a97e4` (v10.30.0) |
| Profile content-eating on /u/me, /u/catsfive | ✅ FIXED `ef47804` (v10.31.0) |
| `.gam-propose-overlay` stacking above preflight / no-dismiss | ✅ FIXED v10.32.0 (STORM #6) |
| `showModal` innerHTML XSS on title param | ✅ FIXED v10.33.0 (STORM #14) |
| Racing `--bb-*` :root blocks (cascade value mismatch) | ✅ FIXED v10.34.0 (STORM #15) |
| `collectDebugSnapshot` leaking 25 log entries | ✅ FIXED v10.35.0 (STORM #12) |
| `/users` triage: hardcoded `.log` selector bypassed fallback chain | ✅ FIXED v10.36.0 |
| Cluster burst alert: no one-click bulk Death Row | ✅ FIXED v10.36.0 — "💀 Death Row all N" |
| `MSG_QUEUE_KEY`/`MSG_QUEUE_MAX` TDZ crash (repeating console warning, same class as v10.6.1) | ✅ FIXED v10.36.1 `86b0fa9` |
| `/users` mount failure silently swallowed + span-count gate ate v10.36.0's own fallback | ✅ FIXED v10.36.2 `94c3108` (this was a gap in this session's own v10.36.0 work) |
| Live extension was stuck at v10.16.43 (~20 versions stale) — root cause of "modtools doesn't run" report | ⚠️ CODE FIXED, awaiting Commander to reload the unpacked extension in Brave |
| Ban/Remove HI-1 wiring — direct `executeBan` violations | ✅ FIXED v10.36.3 `77244a6` — routes through `addToDeathRow`+reaper, "guarded break-glass" |
| `ai_used`/`ai_tone` flags "inert" | ℹ️ INVESTIGATED — write path fine, no consumer exists; real fix is V11-scale (see Open items) |
| Auto-unsticky "regressed since v8.6.4" | ✅ NOT BROKEN — matrix note described a real but long-fixed v8.6.3/v8.6.4 incident; only a stale comment needed fixing (`c950e88`) |
| Auto-unsticky worker paths use AND-logic (age+votes) vs. personal-mode/UI's OR-logic | 🔶 OPEN — needs Commander decision, see Open items |

## Wave 3 — DONE (all 4 items)
| STORM | Item | Version | Commit |
|---|---|---|---|
| #6 | Overlay Esc/click-outside dismiss + z-index `var(--z-modal)` | v10.32.0 | `168b78c` range |
| #14 | `showModal` XSS footgun closed; `textContent` default, `titleIsHtml` opt-in | v10.33.0 | precedes `38a58e5` |
| #15 | Racing `--bb-*` :root blocks — 16 duplicate tokens stripped from `gam-token-sheet` | v10.34.0 | `38a58e5` |
| #12 | Passive last-3-actions strip in `collectDebugSnapshot` (`slice(-25)` → `slice(-3)`) | v10.35.0 | `3803586` |

## Decisions pending (Commander)
1. ~~HI-1: Ban+Remove+Watch wiring~~ — **DONE v10.36.3**, guarded break-glass via `addToDeathRow`+reaper. Watch is still honestly-erroring (no passive-watch endpoint exists yet — unchanged).
2. **Bar taxonomy for Wave 2 — full regroup still open.** v10.36.4 shipped a purely-additive prereq helper (`openCategoryMenu()`) referencing a `QUEUE/ACT/COORD/SYS` (+ SHIELD/CHAT?) shape that differs from this doc's original `SHIELD/MOD/QUEUE/FILTERS/CHAT` recommendation — reconcile which taxonomy is final before the actual ~32-button regroup (#3/#4/#5/#9/#10/#13) lands.
3. ~~Merge `inpage-ui-storm` → master~~ — **DONE**, `7fda929`. All work now ships directly on `master`.
4. **NEW — auto-unsticky AND-vs-OR threshold mismatch.** Both worker paths (`_cronAutoUnstickyScan` L14529-14531, CS-scan-report handler L2815-2816 in `gaw-mod-proxy-v2.js`) require age **AND** votes to queue a candidate; personal-mode (`autoUnstickyTick` in `modtools.js`) and the settings-panel label both promise age **OR** votes. Worker INSERT reason strings literally say `age_and_votes_threshold`, suggesting this was a deliberate (if undocumented) conservative choice for unattended team-wide action rather than a bug. Needs Commander's call: reconcile to OR everywhere, or document the AND-for-team-wide/OR-for-personal split intentionally.

## Open items (severity-ranked)
- **[P1]** Wave 2 bar taxonomy full regroup — needs decision #2 above (taxonomy reconciliation) before the ~32-button move
- **[P1·TIER-1]** HMAC backfill of 459 legacy NULL-hmac audit rows — **script fully verified and ready** (`scripts/backfill-audit-hmac.ps1`, parses clean PS5.1+PS7, worker endpoint confirmed alive/gated via probe). Only missing: Commander pastes `LEAD_MOD_TOKEN` and runs it.
- **[P1]** Auto-unsticky AND/OR threshold mismatch — needs decision #4 above
- **[P2]** `ai_used`/`ai_tone` flags — write path confirmed fine; real gap is the unbuilt "AI acceptance learning loop" (already scoped in `docs/V11_R2_CAT5_METRICS.md` KPI #5 — per-mod tone profiles, history filtered by `ai_used=1`). This is V11-scale product work, not a hotfix — fold into V11 roadmap conversation with Opus rather than patching piecemeal.
- **[P2]** TS-2 drop CSP `unsafe-inline` (38+ inline cssText sites to refactor first)
- **[P2]** UAT_ONBOARDING — only `catsfive` of 15 mods live-tested; CWS `kbhpioj...` blocked on screenshots (14 mods can't onboard)
- **[P3]** Tech-debt TS-1..TS-10 · Chat power features CHAT-1..10 · V11 30-item roadmap · brigade detector + migrations 043/044

## Next steps (ordered)
1. **Commander reloads the extension in Brave** — `brave://extensions` → GAW ModTools → ↻ → F5 any open GAW tab. This is the #1 outstanding item: the live extension was confirmed (via direct console inspection) stuck at v10.16.43, ~20 versions behind. All fixes above are already in source; none have reached the browser yet.
2. **HMAC backfill** — paste `LEAD_MOD_TOKEN` and run the script (fully verified, see Open items).
3. **Bar-taxonomy reconciliation** → confirm final shape, then the full Wave 2 regroup.
4. **Auto-unsticky AND/OR decision** → reconcile worker vs. personal-mode threshold logic.
5. **V11 roadmap kickoff**, including the `ai_used`/`ai_tone` acceptance-learning-loop gap (30-item spec in `FEATURES_MATRIX_v10.5.md §P`; also see `docs/OPUS48_BRAINSTORM_BRIEF.md` Topic 3).

## Cold-resume map
- **Repo:** `D:\AI\_PROJECTS\modtools-ext`. **Active branch: `master`** @ `c950e88` (v10.36.4 + 1 doc-only commit). `inpage-ui-storm` fully merged (`7fda929`) — branch still exists locally/on origin but is stale/archived, no longer the working branch.
- **Recent stack (condensed):** `770e0fe`(v10.36.0)→`7fda929`(merge)→`86b0fa9`(v10.36.1 TDZ)→`10826de`(regression test)→`94c3108`(v10.36.2 P0 /users)→`77244a6`(v10.36.3 HI-1)→`3965ce5`(v10.36.4 taxonomy prereq)→`c950e88`(doc fix).
- **Ship loop:** edit `modtools.js` → `node --check` → run relevant smoke `.mjs` → bump manifest → per-version commit → build-zip.ps1. Note: `tests/regressions/` is a newer convention (per-bug regression tests keyed to `gaw-audit` D1 `bug_reports` rows) alongside the older `scripts/*_smoke_test.mjs` suite — both are live, check both before assuming full coverage.
- **Key files:** `modtools.js` (~1.7MB — targeted reads only). Hot symbols: `_isProfileViewNow` ~L17837 · `closeAllPanels` SEL ~L8017 · `enhanceModmailRead` ~L14246 · `showModal` ~L8470 · `_titleNode` assignment ~L8489 (now `textContent`) · `_SEL_FB`/`trySelect`/`trySelectAll` self-heal selector chain ~L544-615 · `autoUnstickyTick` ~L29470. `background.js`: `modSusMark` ~L3087. Worker: `D:\AI\_PROJECTS\cloudflare-worker\gaw-mod-proxy-v2.js` (separate repo, gated deploy). Smoke: 11 files under `scripts/*_smoke_test.mjs` (162 assertions) + `scripts/_p0_triage_mount_smoke_test.mjs` (18) + `tests/regressions/8.test.js` — all green at v10.36.4.
- **Hard facts:** (1) Real ban is browser-side (Bot Fight Mode); ban routing reuses `addToDeathRow` — HI-1 now resolved via v10.36.3's guarded-break-glass pattern (immediate reaper fire, still queue+audit+idempotent, never a raw `executeBan`). (2) Worker deploy (`gaw-mod-proxy-v2.js`) is a gated separate step — needs Commander's explicit "deploy". (3) D1: 002–031 live, 032 multi-lead live, 043/044 brigade blocked. (4) 15 mods, only `catsfive` live-tested. (5) AI fallback: Llama→Grok→Claude; 500/mod/day, 5000 global/day. (6) CWS `kbhpiojnfnolhpajeccckeikefjfgnkk` blocked on screenshots. (7) STORM #6 overlay uses `--z-modal` (9999995) so preflight (`--z-preflight` = 10000000) and drawer (`--z-drawer` = 2147483600) still render on top. (8) A parallel Claude session (+ Opus 4.8 recon) is actively shipping on this same working directory/repo — check `git log` for the latest state before assuming any doc snapshot (including this one) is fully current.
