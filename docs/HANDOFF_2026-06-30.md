# GAW ModTools — HANDOFF 2026-06-30

**Resume cold from this doc.** Companion: `docs/FEATURES_MATRIX_v10.31.md` (requested vs delivered) · `docs/UI-STORM-2026-06-22.md` (storm top-15) · `docs/inpage-redesign/` (redesign brief + tokens).

---

## Headline — current state (06-30)

**Wave 3 hardening complete.** All 4 Wave 3 STORM items shipped and verified on `inpage-ui-storm`. Branch is at **v10.35.0** @ `3803586`, 32/32 smokes green, parse clean. The fork is ready to merge to master — waiting on Commander confirmation (decision #3 below). Wave 2 (bar taxonomy) and STORM #1 pt2 (ban/Watch wiring) remain gated on the two outstanding Commander decisions.

## Timeline (cumulative)
| Date | Ref | Event |
|---|---|---|
| 06-19 | `ec0e81a..4e65bcc` | inpage-ui WP-01..13 shipped (token system, 13 components, a11y) |
| 06-19 | `74a0109..c7b3b09` | same-day review-fix passes |
| 06-19 | `0bc8146` | fix: ESC-confirm DOM orphan → added to `closeAllPanels` SEL |
| 06-19 | `633dcbb` | safety net: tag `safety/v10.29.0-pre-ui-storm` + fork `inpage-ui-storm`; master FROZEN |
| 06-20 | — | post-delivery docs `/docs/inpage-redesign/` (brief 729L, tokens 228L, prototypes) |
| 06-22 | `3a53a61` | 21-agent UI-STORM synthesis → top-15, 3 waves, 7 fluff cut |
| 06-22 | `317300b` | fix STORM #2 — toolbar artifact (`_gamLinkPreviewEl` orphan) |
| 06-22 | `b6dd92c` | fix STORM #1 pt1 — snack-gate on `r.ok` + SUS live rewire (smoke 11/11) |
| 06-22 | `97321f5` | fix STORM #11 — modmail wrong-user on thread-switch |
| 06-22 | `84a97e4` | v10.30.0 — feed-observer de-jank (smoke 5/5) |
| 06-22 | `ef47804` | v10.31.0 — ROOT-CAUSE profile content-eating fix (smoke 16/16) |
| 06-30 | `6f5abc?` | v10.32.0 — STORM #6: overlay dismiss + z-index drop (`var(--z-modal)`) |
| 06-30 | `168b78c`→ | v10.33.0 — STORM #14: showModal XSS footgun closed (`titleIsHtml` opt-in) |
| 06-30 | `38a58e5` | v10.34.0 — STORM #15: collapse racing `--bb-*` :root blocks |
| 06-30 | `3803586` | v10.35.0 — STORM #12: passive last-3-actions strip in collectDebugSnapshot |

## Regressions & fix status
| Issue | Status |
|---|---|
| ESC-confirm DOM orphan | ✅ FIXED `0bc8146` |
| Link-preview artifact stuck | ✅ FIXED `317300b` |
| Thread-watch silent-success snacks (4 unregistered RPCs) | ⚠️ PARTIAL `b6dd92c` — honest now; Ban/Watch wiring pending HI-1 |
| Modmail banning wrong user on thread-switch | ✅ FIXED `97321f5` |
| Feed-observer jank | ✅ FIXED `84a97e4` (v10.30.0) |
| Profile content-eating on /u/me, /u/catsfive | ✅ FIXED `ef47804` (v10.31.0) |
| `.gam-propose-overlay` stacking above preflight / no-dismiss | ✅ FIXED v10.32.0 (STORM #6) |
| `showModal` innerHTML XSS on title param | ✅ FIXED v10.33.0 (STORM #14) |
| Racing `--bb-*` :root blocks (cascade value mismatch) | ✅ FIXED v10.34.0 (STORM #15) |
| `collectDebugSnapshot` leaking 25 log entries | ✅ FIXED v10.35.0 (STORM #12) |

## Wave 3 — DONE (all 4 items)
| STORM | Item | Version | Commit |
|---|---|---|---|
| #6 | Overlay Esc/click-outside dismiss + z-index `var(--z-modal)` | v10.32.0 | `168b78c` range |
| #14 | `showModal` XSS footgun closed; `textContent` default, `titleIsHtml` opt-in | v10.33.0 | precedes `38a58e5` |
| #15 | Racing `--bb-*` :root blocks — 16 duplicate tokens stripped from `gam-token-sheet` | v10.34.0 | `38a58e5` |
| #12 | Passive last-3-actions strip in `collectDebugSnapshot` (`slice(-25)` → `slice(-3)`) | v10.35.0 | `3803586` |

## Decisions pending (Commander)
1. **HI-1: Ban+Remove+Watch wiring.** Real ban is browser-side (Bot Fight Mode). Recommendation: reuse `addToDeathRow` (72h delayed, idempotent, 20s undo). Watch can stay honestly-erroring until a passive-watch endpoint exists.
2. **Bar taxonomy for Wave 2.** Recommendation: approve `SHIELD / MOD / QUEUE / FILTERS / CHAT`. Unblocks #3/#4/#5/#9/#10/#13.
3. **Merge `inpage-ui-storm` → master.** Recommendation: merge now — Wave 1 + Wave 3 (v10.29.0 → v10.35.0) all verified; 32/32 smokes green. Keep Wave 2 on a follow-up branch. Half-shipping safe fixes beats holding them behind a gated taxonomy.

## Open items (severity-ranked)
- **[P1]** STORM #1 pt2 — wire Ban+Remove+Watch (needs HI-1 decision)
- **[P1]** Wave 2 bar taxonomy — #3/#4/#5/#9/#10/#13 (needs taxonomy confirmation)
- **[P1·TIER-1]** HMAC backfill of 459 legacy NULL-hmac audit rows (`scripts/backfill-audit-hmac.ps1`, needs LEAD_MOD_TOKEN)
- **[P2]** `ai_used`/`ai_tone` flags inert · auto-unsticky regressed since v8.6.4 · TS-2 drop CSP `unsafe-inline`
- **[P2]** UAT_ONBOARDING — only `catsfive` of 15 mods live-tested; CWS `kbhpioj...` blocked on screenshots (14 mods can't onboard)
- **[P3]** Tech-debt TS-1..TS-10 · Chat power features CHAT-1..10 · V11 30-item roadmap · brigade detector + migrations 043/044

## Next steps (ordered)
1. **Commander confirms merge → master.** Run `git checkout master && git merge inpage-ui-storm && git push`. Master has been frozen since 06-19.
2. **Reload extension in Brave** after merge: `brave://extensions` → GAW ModTools → ↻ → F5 GAW tab.
3. **HI-1 decision** → wire Ban+Remove via `addToDeathRow` (completes STORM #1 pt2).
4. **Bar-taxonomy confirmation** → build Wave 2 on a new follow-up branch.
5. **HMAC backfill** — paste LEAD_MOD_TOKEN and run `scripts/backfill-audit-hmac.ps1`.
6. **V11 roadmap kickoff** (30-item spec in `FEATURES_MATRIX_v10.5.md §P`).

## Cold-resume map
- **Repo:** `D:\AI\_PROJECTS\modtools-ext`. **Active branch:** `inpage-ui-storm` @ `3803586` (v10.35.0). **master** FROZEN @ `633dcbb` (tag `safety/v10.29.0-pre-ui-storm`). Both on origin. **No merge to master yet — awaiting decision #3.**
- **Fork stack (condensed):** `317300b`→`b6dd92c`→`97321f5`→`84a97e4`(v10.30.0)→`ef47804`(v10.31.0)→v10.32.0→v10.33.0→`38a58e5`(v10.34.0)→`3803586`(v10.35.0).
- **Ship loop:** edit `modtools.js` → `node --check` → run relevant smoke `.mjs` → bump manifest → per-version commit → build-zip.ps1.
- **Key files:** `modtools.js` (~1.7MB — targeted reads only). Hot symbols: `_isProfileViewNow` ~L17837 · `closeAllPanels` SEL ~L8017 · `enhanceModmailRead` ~L14246 · `showModal` ~L8470 · `_titleNode` assignment ~L8489 (now `textContent`). `background.js`: `modSusMark` ~L3087. Smoke: `scripts/_thread_watch_snack_gate_smoke_test.mjs` (11/11), `_observer_scoping_smoke_test.mjs` (5/5), `_profile_view_detect_smoke_test.mjs` (16/16). **32/32 green at v10.35.0.**
- **Hard facts:** (1) Real ban is browser-side (Bot Fight Mode); ban routing must reuse `addToDeathRow`, never an immediate-ban path — HI-1 gate. (2) Worker deploy (`gaw-mod-proxy-v2.js`) is a gated separate step. (3) D1: 002–031 live, 032 multi-lead live, 043/044 brigade blocked. (4) 15 mods, only `catsfive` live-tested. (5) AI fallback: Llama→Grok→Claude; 500/mod/day, 5000 global/day. (6) CWS `kbhpiojnfnolhpajeccckeikefjfgnkk` blocked on screenshots. (7) STORM #6 overlay uses `--z-modal` (9999995) so preflight (`--z-preflight` = 10000000) and drawer (`--z-drawer` = 2147483600) still render on top.
