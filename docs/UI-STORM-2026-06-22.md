# ModTools UI-Taming Storm — 2026-06-22

**Source:** 21-agent grounded workflow (5 recon readers → 15 named specialists → 1 synthesis judge). 60 candidates → ranked top-15, 7 cut as fluff. Run on the live v10.29.0 code.

**Safety net (created before any work):**
- Tag `safety/v10.29.0-pre-ui-storm` @ `633dcbb` — rollback target.
- Branch `inpage-ui-storm` — fork for all UI-storm work. `master` frozen.

**Trigger:** Commander — the recent `inpage-ui` WP-03→WP-13 redesign "went forward but also backward." It shipped GAM_TOK tokens + component migrations but **skipped both named asks** (labeled-category bar, the toolbar artifact) and left a family of silent-success no-ops.

---

## THE ARTIFACT (ask B) — ROOT-CAUSED + FIXED
`_gamLinkPreviewEl` (`div.gam-mc-link-preview`, modtools.js:18078, CSS :26644, z-index 99999997) — a ModChat link hover-preview (OG title+desc+host). Body-appended once, only class-toggled `gam-show`, **never removed**, absent from `closeAllPanels()` SEL. Stuck shown over `#gam-status-bar` after a ModChat close / anchor-detach mid-hover with no mouseout.
**FIXED on fork @ `317300b`:** added `.gam-mc-link-preview` to the closeAllPanels SEL (`e.remove()` @ :8017, 19 routes) + `_gamEnsureLinkPreviewEl` now requires `.isConnected` (self-heals the detached cache). No version bump (Wave-1 WIP).

---

## TOP 15 — sequenced into 3 waves

### 🔴 WAVE 1 — Stop the tool lying / banning the wrong person (surgical, no design taste)
- **#1** `M·11` **Gate Thread-Watch success snacks on `r.ok`.** `_onBanAndRemove` (:32582), bulk ban/watch (:32620/:32642) call 4 RPC names absent from background.js (`modBanUser`/`modCommentRemoveBatch`/`modWatchUser`/`modMarkSus`) → resolve `{ok:false}`, catch (:32590) only fires on reject → green "Banned + removed" snack while nothing happened. Fix: gate every snack on `r.every(x=>x&&x.ok)` (pattern exists @ :13850) + register the 4 handlers → real routes (verify payload shapes). **[KNOWN — already a task chip.]**
- **#2** `S·12` **Kill the artifact.** ✅ DONE @ `317300b`.
- **#6** `S·6` **SM-Watcher proposal overlay dismissable + z-index drop.** `renderInlineProposalReview` (:31916) / confirmModal (:32037) append a full-screen scrim, no Escape/outside-click, no teardown ref → orphans on re-init; `#gam-propose-overlay` z-index 2147483600 sits ABOVE the permanent-ban preflight confirm (9999995, :3819) → a ban confirm can render under it. Fix: ESC+outside-click dismiss, module-scope ref + SEL entry, z-index→9999994, gate the Punt/Veto `.then(dismiss)` on r.ok.
- **#11** `S·1` **Stop modmail banning the wrong user on thread-switch.** `#gam-mm-bar` (:14246) keeps thread A's `sender` bound after SPA-nav to thread B → Ban on B bans A's author. Fix: unconditional `#gam-mm-bar?.remove()` at top of `enhanceModmailRead` + pathname-guard the 800ms retry (:14258).

### 🟡 WAVE 2 — Tame the bar (asks A + lowest bar). NEEDS COMMANDER'S TAXONOMY STEER.
- **#3** `L·8` **Group ~32 bar children → ~5-6 LABELED CATEGORY buttons** (non-destructive wrap, handlers verbatim, behind `gam_status_bar_orientation` flag). Storm taxonomy: `SHIELD / MOD / QUEUE / FILTERS / CHAT`. Constructor :23605, CSS :24921. Upward-menu machinery exists (`bottom=innerHeight-r.top+6`). **MUST ship with #4 + #5.**
- **#4** `S·3` **Live-count chips stay OUT of menus as visible badge pills.** `#gam-dr-count` 💀 (:23215), `#gam-raid-icon` 🤖 (:23234), `#gam-siren-count` 🚨 (:23248), `#gam-sticky-chip` (:23635). Words for cold actions, bare numbers for hot state.
- **#5** `S·4` **Horizontal viewport clamp on the shared upward-menu helper.** Anchoring is vertical-only today → fat category menus clip at bar ends. ~25-line `openCategoryMenu(anchor,items)` with left-clamp + max-height scroll + closeAllPanels-on-open. Prereq for #3.
- **#9** `S·2` **Accessible name on every bar control** (emoji are sole content; SR says "skull" for a ban button). aria-label keyed off the recon IDs; labels migrate into menuitems when #3 lands.
- **#10** `M·1` **Category buttons = real ARIA menu-button widget** (aria-haspopup/expanded, role=menu/menuitem, arrow/Home/End, Escape + focus-return). Folds into #5's helper.
- **#13** `M·3` **Drop bar to `bottom:8px` with snacks/accordions/padding re-anchored in lockstep.** bottom:0 blocked by 3 couplings: snacks bottom:14px (:8383/:24429), accordions bottom:36/46 (:23818/:23914), body paddingBottom 56 (:23701). Move all in ONE diff.

### 🟢 WAVE 3 — Harden & de-jank
- **#7** `M·2` **Scope feed-strip injection to added subtrees.** `_gamBodyObs` (:15103) → `injectAllStrips` runs `document.querySelectorAll('.post,.comment')` full-page on every added node → O(added×total) per scroll tick. Give it a root param (pattern exists: `_susApplyDecorationsOnNodes`); keep full sweep only for initial load.
- **#8** `S·2` **Debounce 3 observers** (`_schedObs` :29896, `_actionObs` :27327, Triage /users :16882 — rebuilds whole UI per virtual-scroll row). Crawler obs (:28547) already debounced @ 1500ms — copy the idiom. Promote to module-scope `let` for a disconnect path.
- **#12** `M·1` **Passive "last 3 actions" strip on the bar.** Reads existing log buffer (`logAction` :12115, undo ring :5332) — zero new compute. Turns #1's silent failures into something you can SEE. Bound to last-3, no activity-feed creep.
- **#14** `M·1` **Close the innerHTML title XSS gap.** `_titleNode.innerHTML=title` (:8488); usernames flow in from worker responses. `escapeHtml` exists — call-site discipline; add a separate `titleHtml` param for the 2-3 sites needing markup.
- **#15** `S·2` **Collapse the two racing `--bb-*` :root blocks.** Static GAM_CSS (:25705) vs runtime `__injectTokenAndStateStylesheet` (:27074) redefine the same vars with CONFLICTING values → cascade race by load order. Alias legacy `--bb-*` → GAM_TOK in one block, delete the other (~30 lines, NOT the 942-ref migration). Flag the hue shift for eyeball.

---

## CUT AS FLUFF (7)
Retry-toast component (additive UX, the detection in #1 is the load-bearing half), RPC self-test health dashboard (framework creep, duplicates #1's honest errors), lazy-build menus (premature, fold into #3), "menu-shell foundation epic" (dup of #5/#10/#6), queue-zero settle animation + menu fade motion (Whimsy's own admit: polish on a high-stakes surface), crawler AbortController cap (real but below cut line).

---

## RESUME
- Full synthesis JSON: workflow run `wf_4641ceef-622` output.
- Next drive: Wave 1 remaining = **#1 (snack-gate), #6 (overlay), #11 (modmail)** — all surgical, on the fork.
- Wave 2 gated on Commander's bar-taxonomy decision (confirm/redirect `SHIELD/MOD/QUEUE/FILTERS/CHAT`).
- Ship loop when a wave is complete: version bump → CHANGELOG → `node --check` → build-zip → mirror. Then push fork.
