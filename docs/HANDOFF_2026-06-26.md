# GAW ModTools — HANDOFF 2026-06-26

**Resume cold from this doc.** Companion: `docs/FEATURES_MATRIX_v10.31.md` (requested vs delivered) · `docs/UI-STORM-2026-06-22.md` (storm top-15) · `docs/inpage-redesign/` (redesign brief + tokens).

---

## Headline — the week (06-19 → 06-22)
A design-led redesign that **went forward and backward at once.** On **06-19** the `inpage-ui` redesign landed all 13 work-packages (`ec0e81a..4e65bcc`): a unified 20-token `GAM_TOK` system replacing ~193 hex literals, a z-ladder, an elevation contract, and Mod Console a11y — plus same-day review-fix passes. But it **skipped both named asks** (labeled-category bar, kill the toolbar artifact) and **exposed/left a family of silent-success no-ops.** On **06-22** a 21-agent grounded **UI-STORM** produced a ranked top-15 across 3 waves and shipped **Wave 1 + Wave 3 de-jank on an isolated fork** (`inpage-ui-storm`). `master` is frozen at `safety/v10.29.0-pre-ui-storm` (633dcbb) as the rollback baseline. **Wave 2** (bar taxonomy) and **STORM #1 pt2** (ban/Watch wiring) are gated on Commander decisions.

## Timeline
| Date | Ref | Event |
|---|---|---|
| 06-19 | `ec0e81a..4e65bcc` | inpage-ui WP-01..13 shipped (token system, 13 components, a11y) |
| 06-19 | `74a0109..c7b3b09` | same-day review-fix passes |
| 06-19 | `0bc8146` | **regression fix:** ESC-confirm DOM orphan → added to `closeAllPanels` SEL |
| 06-19 | `633dcbb` | **safety net:** tag `safety/v10.29.0-pre-ui-storm` + fork `inpage-ui-storm`; master frozen |
| 06-20 | — | post-delivery docs `/docs/inpage-redesign/` (brief 729L, tokens 228L, prototypes) |
| 06-22 | `3a53a61` | 21-agent UI-STORM synthesis → top-15, 3 waves, 7 fluff cut (`wf_4641ceef-622`) |
| 06-22 | `317300b` | **fix** STORM #2 — toolbar artifact (`_gamLinkPreviewEl` orphan) |
| 06-22 | `b6dd92c` | **fix** STORM #1 pt1 — snack-gate on `r.ok` + SUS live rewire (smoke 11/11) |
| 06-22 | `97321f5` | **fix** STORM #11 — modmail wrong-user on thread-switch |
| 06-22 | `84a97e4` | **v10.30.0** — feed-observer de-jank (smoke 5/5) |
| 06-22 | `ef47804` | **v10.31.0** — ROOT-CAUSE profile content-eating fix (smoke 16/16) |

## Regressions & fix status (the "went backward")
| Issue | Origin | Status |
|---|---|---|
| ESC-confirm DOM orphan (#mc-esc-confirm left attached on non-button teardown) | WP-02 reserved-footer-slot w/o teardown sweep | ✅ FIXED `0bc8146` (smoke 9) |
| Link-preview artifact stuck over status bar | pre-existing; surfaced by storm; a named-but-skipped ask | ✅ FIXED `317300b` |
| Thread-watch silent-success snacks (4 unregistered RPCs → fake "Banned") | pre-existing silent-failure pattern | ⚠️ PARTIAL `b6dd92c` — honest now; Ban/Watch wiring pending HI-1 |
| Modmail banning wrong user on thread-switch | pre-existing early-exit-on-existing-bar | ✅ FIXED `97321f5` (Commander live-confirmed) |
| Feed-observer jank (5 observers, full-doc sweeps per mutation) | pre-existing inefficiency under load | ✅ FIXED `84a97e4` (v10.30.0) |
| Profile content-eating on /u/me, /u/catsfive (~12+ sessions recurring) | chronic whitelist-falling-out-of-date | ✅ FIXED `ef47804` (v10.31.0) — structural |

## Decisions pending (Commander) — my recommendation in **bold**
1. **HI-1: how to wire Ban+Remove + Watch thread-watch buttons?** Real ban is browser-side (Bot Fight Mode — no server ban-proxy). → **Reuse `addToDeathRow` (72h delayed, idempotent, 20s undo) for Ban+Remove — NOT a new immediate-ban path.** For Watch, confirm whether `modSniperArm` (auto-ban primitive) is acceptable or leave honestly-erroring until a true passive-watch endpoint exists. Buttons error honestly today, safe to leave until the call.
2. **Bar taxonomy for Wave 2.** → **Approve the storm's `SHIELD / MOD / QUEUE / FILTERS / CHAT`.** Non-destructive wrap; unblocks all of Wave 2 (#3/#4/#5/#9/#10/#13, ship together). Confirm or redirect the labels.
3. **Merge posture for the fork.** → **Merge the verified Wave 1 + Wave 3 fixes to master now** (all regressions + de-jank, every commit `node --check` + smoke green); keep Wave 2 on a follow-up branch gated on the taxonomy. Half-shipping safe fixes beats holding them behind a gated taxonomy.

## Open items (severity-ranked)
- **[P1]** STORM #1 pt2 — wire Ban+Remove+Watch (blocks full snack-gate merge; needs HI-1 decision)
- **[P1]** Wave 2 bar taxonomy — #3/#4/#5/#9/#10/#13 (gated on taxonomy; a named ask the redesign skipped)
- **[P1]** STORM #14 — close `innerHTML` title XSS (`modtools.js:8488`, usernames from worker; add `titleHtml` param + escapeHtml)
- **[P1·TIER-1]** HMAC backfill of 459 legacy NULL-hmac audit rows (`scripts/backfill-audit-hmac.ps1`, needs LEAD_MOD_TOKEN)
- **[P2]** STORM #6 SM-Watcher overlay dismiss/z-drop · STORM #12 passive last-3-actions strip · STORM #15 collapse racing `--bb-*` blocks
- **[P2]** `ai_used`/`ai_tone` flags inert (callers pass no opts) · auto-unsticky regressed since v8.6.4 · TS-2 drop CSP `unsafe-inline`
- **[P2]** UAT_ONBOARDING — only `catsfive` of 15 mods live-tested; CWS `kbhpioj...` blocked on screenshot upload (14 mods can't onboard)
- **[P3]** Tech-debt TS-1..TS-10 · Chat power features CHAT-1..10 + V11 30-item roadmap · brigade detector + migrations 043/044

## Next steps (ordered)
1. **Live-verify the fork** in Brave: modmail thread-switch, snack-gate, observer smoothness on long-thread scroll, /u/me + /u/catsfive show content. Re-run all 3 smoke suites + `node --check` on `ef47804`.
2. Get the **HI-1 decision** → wire Ban+Remove via `addToDeathRow`, resolve Watch (completes STORM #1 pt2).
3. Get the **bar-taxonomy** confirmation → build Wave 2 (#3/#5 structural first, then #4/#9/#10/#13) on a follow-up branch.
4. **Merge `inpage-ui-storm` → master** once live-verified; push (master frozen at 633dcbb).
5. Wave 3 hardening: XSS (#14), last-3-actions (#12), racing `--bb-*` (#15), SM-Watcher overlay (#6).
6. Standing backlog: HMAC backfill (TIER-1), TS-2 CSP, then TS-1..10.
7. V11 roadmap kickoff (30-item spec in `FEATURES_MATRIX_v10.5.md §P`).

## Cold-resume map
- **Repo:** `D:/AI/_PROJECTS/modtools-ext`. **Active branch:** `inpage-ui-storm` @ `ef47804` (v10.31.0). **master** FROZEN @ `633dcbb` (tag `safety/v10.29.0-pre-ui-storm`). Both on origin. **No merge to master yet.**
- **Fork stack (old→new):** `317300b`→`b6dd92c`→`97321f5`→`84a97e4`(v10.30.0)→`ef47804`(v10.31.0).
- **Ship loop:** edit `modtools.js` → `node --check` → run relevant smoke `.mjs` → bump manifest → per-version commit → `/ext-release` (modtools-release skill). Drive mirror `E:\My Drive\_PROJECTS\modtools-ext\` (last 2).
- **Key files:** `modtools.js` (~1.7MB — targeted reads only). Hot symbols: `_isProfileViewNow` :17837 · `closeAllPanels` SEL :8017 · `_gamLinkPreviewEl` :18078 · `enhanceModmailRead` :14246 · `showModal` :8267 · `_titleNode.innerHTML` :8488 (XSS). `background.js`: `modSusMark` :3087. Smoke: `scripts/_thread_watch_snack_gate_smoke_test.mjs` (11/11), `_observer_scoping_smoke_test.mjs` (5/5), `_profile_view_detect_smoke_test.mjs` (16/16).
- **Hard facts:** (1) Real ban is browser-side (Bot Fight Mode); ban routing must reuse `addToDeathRow`, never an immediate-ban path — this is the HI-1 gate. (2) Worker deploy (`gaw-mod-proxy-v2.js`) is a gated separate step, classifier-needs Commander "deploy". (3) D1: 002–031 live, 032 multi-lead live, 043/044 brigade blocked. (4) 15 mods, only `catsfive` live-tested. (5) AI fallback: Llama→Grok→Claude; 500/mod/day, 5000 global/day. (6) CWS `kbhpiojnfnolhpajeccckeikefjfgnkk` blocked on screenshots. (7) The redesign "went forward and backward" — skipped 2 named asks + left silent no-ops the storm caught.
